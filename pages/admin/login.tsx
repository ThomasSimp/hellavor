import Layout from '../../components/Layout';
import Login from '../../sections/admin/Login';

const signup = () => {
    return (
        <Layout>
            <Login />
        </Layout>
    );
};

export default signup;
