import Layout from '../../components/Layout';
import Login from '../../sections/admin/Login';

const login = () => {
    return (
        <Layout>
            <Login />
        </Layout>
    );
};

export default login;
