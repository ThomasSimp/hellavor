import Layout from "../components/Layout";
import Jobs from '../sections/Jobs'

const JobsPage = () => {
    return (
        <Layout>
            <Jobs />
        </Layout>
    );
};

export default JobsPage;
